{
  "name": "@petlog/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "prisma generate",
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "docker:up": "docker compose -f ../docker-compose.yml up -d",
    "docker:down": "docker compose -f ../docker-compose.yml down",
    "docker:logs": "docker compose -f ../docker-compose.yml logs -f db",
    "docker:login": "dotenv -e .env -- bash -c 'REGION=\"${AWS_REGION:-ap-northeast-2}\"; aws ecr get-login-password --region \"$REGION\" | docker login --username AWS --password-stdin \"$(aws sts get-caller-identity --query Account --output text).dkr.ecr.$REGION.amazonaws.com\"'",
    "docker:build": "docker build -f Dockerfile -t petlog-backend ..",
    "docker:push": "dotenv -e .env -- bash -c 'REGION=\"${AWS_REGION:-ap-northeast-2}\"; ENV=\"${PETLOG_ENV:-dev}\"; ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text); docker tag petlog-backend:latest \"$ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/petlog-backend-$ENV:latest\" && docker push \"$ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/petlog-backend-$ENV:latest\"'",
    "docker:force-redeploy": "dotenv -e .env -- bash -c 'REGION=\"${AWS_REGION:-ap-northeast-2}\"; ENV=\"${PETLOG_ENV:-dev}\"; aws ecs update-service --region \"$REGION\" --cluster \"petlog-cluster-$ENV\" --service \"petlog-backend-$ENV\" --force-new-deployment >/dev/null'",
    "docker:deploy": "npm run docker:login && npm run docker:build && npm run docker:push && npm run docker:force-redeploy",
    "deploy": "npm run docker:deploy",
    "seed:report-requirements": "ts-node -T scripts/seed-report-requirements.ts",
    "seed:report-history": "ts-node -T scripts/seed-report-history.ts",
    "seed:illness-symptoms": "ts-node -T scripts/seed-illness-symptoms.ts"
  },
  "dependencies": {
    "@apollo/server": "^5.0.0",
    "@as-integrations/express5": "^1.1.2",
    "@nestjs/apollo": "^13.0.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/graphql": "^13.0.0",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/passport": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@nestjs/swagger": "^11.4.5",
    "@petlog/ai": "*",
    "@petlog/storage": "*",
    "@prisma/client": "^6.0.0",
    "bcrypt": "^5.1.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "cookie-parser": "^1.4.7",
    "graphql": "^16.9.0",
    "openai": "^6.45.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "passport-local": "^1.0.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@types/bcrypt": "^5.0.2",
    "@types/cookie-parser": "^1.4.8",
    "@types/express": "^5.0.0",
    "@types/multer": "^2.1.0",
    "@types/node": "^22.0.0",
    "@types/passport-jwt": "^4.0.1",
    "@types/passport-local": "^1.0.38",
    "dotenv-cli": "^11.0.0",
    "jest": "^29.7.0",
    "prisma": "^6.0.0",
    "ts-jest": "^29.2.0",
    "ts-node": "^10.9.2"
  }
}
