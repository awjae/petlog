# 앱이 로드할 URL. 배포 도메인이거나, 로컬 검증 시 frontend 프로덕션 빌드 주소.
# 에뮬레이터에서 호스트 Mac의 localhost를 가리키려면 10.0.2.2를 쓴다.
# 반드시 `next build && next start`로 띄운 프로덕션 서버를 가리켜야 한다 — `next dev`(Turbopack
# HMR)는 에뮬레이터 네트워크 경로에서 웹소켓 연결에 실패해 ~80초마다 페이지가 강제 리로드된다.
MOBILE_APP_URL=http://10.0.2.2:3001
